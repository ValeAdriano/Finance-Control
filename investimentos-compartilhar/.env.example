# Só para manutenção (publicar a Edge Function, rodar o schema).
# O app em si não usa este arquivo: abre direto pelo index.html.

# Token pessoal da conta Supabase (Account > Access Tokens). Dá acesso a
# todos os projetos da conta — apague depois de usar.
SUPABASE_ACCESS_TOKEN=

# Chave secreta do projeto (Project Settings > API Keys). Nunca no navegador.
SUPABASE_SECRET_KEY=
